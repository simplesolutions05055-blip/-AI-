# סשן 01.09.2026 — הקמת משתמשים ע"י אדמין, ניקוי תפריט האדמין, אכיפת מגבלות, והסרת GREEN-API

מבוסס על תמלול פגישות מאור עטיה (`docs/תמלול-מאור-עטיה-2026-09-01.md`, הקלטות 1–3).
כל שינוי כאן עלה ל-`main` ונפרס לפרודקשן (`tgropjisnheppsxejfdn`).

קומיטים בסשן: `07be053`, `01118fe`, `c27760b`, `eaa558e`, `2ab1f83`.

---

## 1. חסימת הרשמה עצמית + הקמת משתמשים ע"י אדמין (`07be053`)

**דרישה (הקלטה 2):** "אין הרשמה רגילה". רק אדמין מקים משתמש, קובע לו מייל+סיסמה,
משייך למותג קיים או פותח מותג חדש. המשתמש נכנס ומתחיל לעבוד — בלי טופס פרטים,
בלי קוד אימות במייל, בלי "AI slop".

### Frontend

| קובץ | שינוי |
|---|---|
| `src/App.tsx` | הוסר route `/signup` + import של `SignupPage` (הקובץ עצמו נשאר, לא בשימוש). |
| `src/pages/LoginPage.tsx` | הוסר קישור "אין לך חשבון? הירשם" + ה-fetch של `public_signup_visible`. "שכחתי סיסמה" נשאר. |
| `src/components/TitleManager.tsx` | הוסרה כותרת ה-tab של `/signup`. |
| `src/pages/admin/PermissionsPage.tsx` | ראה למטה. |

### `PermissionsPage.tsx`

- **הוסר** טאב "הזמנות" על כל מרכיביו: `InvitesTab`, `CreateInviteModal`,
  `InviteShare`, `inviteUrl`, `InviteRow`, ה-fetch של `brand_invites`,
  הפונקציות `createInvite` / `copyLink` / `toggleRevoke` / `deleteInvite`,
  import של `QRCodeSVG` ו-`randomUUID`.
- **נוסף** כפתור "+ צור משתמש" בטאב "משתמשים" → `CreateUserModal`:
  - שדה מייל (= שם משתמש)
  - שדה סיסמה + כפתור **"צור אוטומטית"** (`generatePassword()` — 14 תווים,
    `crypto.getRandomValues`, א"ב ללא תווים מבלבלים) + כפתור הצג/הסתר
  - רדיו: "מותג קיים" (dropdown של `brands`) / "מותג חדש" (שדה שם)
  - שליחה → `db.functions.invoke('admin-create-user', ...)`
  - בהצלחה עם `brand_created` → toast + `navigate('/admin/branding')` להשלמת
    לוגו/צבעים/מסמכים.
- מיפוי שגיאות edge → הודעות עברית (`invalid_email`, `weak_password`,
  `email_taken`, `brand_exists`, `brand_not_found`, `forbidden`...).

### Edge function חדשה: `supabase/functions/admin-create-user/index.ts`

- אימות: הקורא חייב להיות `admin` (JWT → `profiles.role`), כמו `delete-user`.
- ולידציה: פורמט מייל, סיסמה ≥ 8.
- **מותג נפתר קודם** (כדי שכשל לא ישאיר auth user יתום):
  - `existing` → בדיקה שהמותג קיים.
  - `new` → בדיקת כפילות שם (`normalizeForMatch`, trim + lowercase + הסרת
    פיסוק) מול כל ה-brands. ה-DB ממילא אוכף `unique index brands_name_idx on
    lower(name)`. אם קיים → `brand_exists`. אחרת `insert { name, created_by,
    official_name: name, short_name: name }` (שאר העמודות ברירת מחדל).
- `auth.admin.createUser({ email, password, email_confirm: true })`.
  אם נכשל ו-`brand_created` → מוחקים את המותג שנוצר.
- `user_brands` upsert + `profiles.update({ can_create_outputs: true,
  onboarding: { details_done: true, brand_done: true } })` — כדי
  ש-`needsOnboardingGate` יחזיר `false` והמשתמש ינחת ישר באפליקציה בלי
  טופס פרטים.
- `logEvent('admin_user_created')`. מחזיר `{ ok, user_id, brand_id, brand_created }`.

### `supabase/functions/signup/index.ts`

- נוסף kill-switch בראש ה-handler: `if (Deno.env.get('SIGNUP_ENABLED') !==
  'true') return json(req, { error: 'signup_disabled' });`
- הקוד המקורי נשאר מתחת (לא נמחק, לפי בקשת המשתמש). ה-guard משתמש ב-env
  ולא ב-const כדי ש-`deno check` לא יסמן את השאר כ-unreachable.
- **אומת בפרודקשן:** `curl -X POST .../functions/v1/signup` → `{"error":"signup_disabled"}`.

### לא נעשה (לפי בקשת המשתמש)

- אין `must_reset_password` / flow כניסה ראשונה. האדמין קובע סיסמה סופית.
- טאב "הזמנות" הישן: `resolve-invite` וטבלת `brand_invites` **נשארו בקוד/DB**
  (לא מחיקה הרסנית) — פשוט לא נגישים מה-UI.

### אבטחה

הסיסמה לא נשמרת גולמית ולא ניתנת לצפייה אחרי היצירה. האדמין רואה אותה פעם אחת
במודאל (או מקליד בעצמו). איפוס בהמשך — פריט עתידי (כפתור "אפס סיסמה" ייעודי
במסך פרטי משתמש עדיין לא קיים).

---

## 2. הסרת הבאנר הצהוב של האונבורדינג (`01118fe`)

**דרישה:** "תעיף את הבאנר הצהוב הזה" (screenshot: "עוד לא השלמתם את הגדרת
המותג — מומלץ להוסיף מסמכים וקבצים").

`src/pages/admin/AdminLayout.tsx`:
- הוסר import של `OnboardingBanner`
- הוסר `shouldShowOnboardingBanner` מה-import של `@/lib/onboarding`
- הוסר `const showBanner = ...` ורינדור `{showBanner && <OnboardingBanner .../>}`

`OnboardingBanner.tsx` ו-`shouldShowOnboardingBanner` נשארו בקוד (לא בשימוש).
`needsOnboardingGate` נשאר פעיל (חוסם משתמש בלי מותג/פרטים — לא רלוונטי
למשתמש שאדמין הקים, כי שם הדגלים מסומנים).

---

## 3. הסתרת מסכי הפקת-תוכן מהאדמין (`c27760b`)

**דרישה:** "תסתיר את הכפתורים האלה בתפריט לאדמין... לא צריך שתהיה לו גישה
לעמודים האלה" (screenshots: אייקוני גאנט, תכנון שנתי, תוצרים).

הרציונל תואם להקלטה 2: האדמין לא מפיק תוצרים בעצמו — יש לו משתמש נפרד לזה,
ובעתיד "כניסה למשתמש" (impersonation).

### `src/components/AdminNav.tsx`

- `userOnly: true` על שלושת ה-links: `/admin/holidays` (גאנט),
  `/admin/annual-planner` (תכנון שנתי), `/admin/files` (תוצרים).
  (`visibleSections` כבר מסנן `userOnly` כשה-user הוא admin.)
- סקשן "תוכן" מתרוקן לאדמין → מסונן החוצה אוטומטית.
- `AdminBottomNav` — `mobileHrefs` לאדמין שונה מ-
  `['/admin', '/admin/files', '/admin/holidays', '/admin/annual-planner']`
  ל-`['/admin', '/admin/permissions', '/admin/requests', '/admin/branding']`.

### `src/pages/admin/AdminLayout.tsx`

- נוסף חסימת route ישיר לאדמין:
  ```ts
  const ADMIN_BLOCKED_PREFIXES = ['/admin/holidays', '/admin/annual-planner', '/admin/files', '/admin/production'];
  if (isAdmin && ADMIN_BLOCKED_PREFIXES.some((p) => pathname.startsWith(p))) {
    return <Navigate to="/admin" replace />;
  }
  ```
  (חוסם גם `/admin/files/:id/revise` כי הוא מתחיל ב-`/admin/files`.)

---

## 4. הסרת GREEN-API (`eaa558e` + `2ab1f83`)

**דרישה (הקלטה 3):** "וואטסאפ... Green API, להעיף... אפשר להעיף". וב-follow-up
בסשן: "תמחק את אלה ואת כל הקוד שקשור לgreen api באתר".

הרקע: Smart Send הוא ה-gateway היחיד החי כבר מאוגוסט. GREEN-API היה
transport ישן. ה-group webhook כבר היה hard-retired (`const retired = true`
→ 410).

### שלב א' (`eaa558e`)

| פעולה | פרטים |
|---|---|
| נמחק | `supabase/functions/greenapi-status/` (edge function — health check) |
| נמחק | `supabase/functions/whatsapp-group-webhook/` (retired 410) + הרשומה שלו ב-`supabase/config.toml` |
| נמחק | `supabase/functions/_shared/greenapi.ts` (client — היחיד שייבא אותו היה whatsapp-group-webhook) |
| נמחק | פאנל "חיבור GREEN-API" מטאב WhatsApp בהגדרות + `GreenApiStatus` interface + `checkGreenApi` + `stateHint` + `StatusRow` + state `gwStatus`/`gwChecking`/`lastKnownState` |
| נערך | `_shared/safeFetch.ts` — הוסרו `.greenapi.com` / `.green-api.com` מה-allowlist + `GREENAPI_API_URL` מרשימת ה-env |
| נערך | `twilio-webhook/index.ts` — כותרת קובץ (הסרת אזכור whatsapp-group-webhook) |
| נמחק מה-remote | `supabase functions delete greenapi-status` + `whatsapp-group-webhook` |

הגארד ב-`SettingsPage` שמדלג על מפתח `greenapi_instance_state` בטעינת ה-settings
**נשאר** — כדי שערך runtime ישן לא יידרס חזרה ב-save().

### שלב ב' (`2ab1f83`) — הסרת המעגל האחרון

| פעולה | פרטים |
|---|---|
| נמחק | `supabase/functions/_shared/instanceState.ts` (circuit-breaker על בריאות ה-instance) |
| נערך | `_shared/worker.ts` — הוסר `import { isSendBlocked }` + הבלוק ב-`sendOut` שחסם שליחה כש-`isSendBlocked` (כשל שליחה של Smart Send ממילא נזרק ומטופל ב-`needs_attention`) |
| נערך | `conversation-maintenance/index.ts` — הוסר `import { recordInstanceState }` + `getChannelState` + בלוק "0. Gateway health" |
| נערך | `_shared/whatsapp.ts` — הוסר export `getChannelState`; ניקוי כותרת ה-transport switch + הערת downloadMedia |
| נערך | `_shared/whatsappText.ts`, `_shared/group.ts` — הערות (GREEN-API → Smart Send / היסטורי) |

**נשאר בכוונה (לא GREEN-API):**
- `_shared/group.ts` — שכבת ניתוב קבוצות. שזורה ב-`flow.ts` ו-`worker.ts`.
  משמשת את **הקבוצה המדומה למותג** (`brand-<id>@sim.group`) — פיצ'ר חי.
  שליחה לקבוצת WhatsApp אמיתית זורקת (Smart Send לא תומך).
- `group-chat` (edge function) — endpoint של הקבוצה המדומה. חי.
- `_shared/twilio.ts` — קוד מת היסטורי (transport switch בלי route אליו).
  יש בו שגיאות טיפוס קדומות; לא נגעתי.
- `smartsend.getState()` — נשאר מוגדר (לא בשימוש כרגע, לא GREEN-API).

**אימות:** `grep -rniE "green.?api|greenapi|instanceState|getChannelState|
isSendBlocked|recordInstanceState"` על `src` + `supabase` → נשארו רק
הערות + הגארד המכוון על המפתח הישן.

---

## 5. אכיפת מגבלות — עמוד "הגדרות → מגבלות" (`2ab1f83`)

**דרישה (הקלטה 3):** "אני חייב... מגבלות האלה, אני רוצה לבדוק שהן נאכפות בכלל".

### מצב לפני הסשן

| מגבלה | מפתח | נאכף? | היכן |
|---|---|---|---|
| סבבי שאלות מקסימלי | `question_rounds.max` | ✅ | `worker.ts` (`roundsRemaining`) |
| ניסיונות יצירה מקסימלי | `generation_attempts.max` | ✅ | `worker.ts` |
| ניסיונות תמונה מקסימלי | `image_generation_attempts.max` | ✅ | `worker.ts` |
| תקרת עלות לבקשה ($) | `request_budget_usd.max` | ✅ | `worker.ts` → `needs_attention` |
| תקרת הוצאה יומית לכל המערכת ($) | `rate_limits.daily_budget_usd` | ✅ | `budgetGuard.ts` → `abuseGuard` |
| הודעות ל-24 שעות | `rate_limits.messages_per_24h` | ✅ | `smartsend-webhook` (חוסם לפני עיבוד) |
| **יצירות ל-24 שעות** | `rate_limits.generations_per_24h` | ❌ | אין קוד |

### מה נעשה

`_shared/abuseGuard.ts` → `enforceAiLimit`, אחרי בדיקות `ai_hour`/`ai_day`:

```ts
if (opts.kind === 'generation') {
  const rateLimits = await getSettingOr<{ generations_per_24h?: number }>(database, 'rate_limits', {});
  const genMax = Number(rateLimits.generations_per_24h ?? 0);
  if (genMax > 0) {
    const used = await countEvents(database, key, 'generation_day', 86400);
    if (used >= genMax) {
      // log abuse_guard_rate_limited + throw AbuseGuardError('generations_per_24h_exceeded', ...)
    }
  }
}
```

- נספר מ-event חדש `generation_day` (נרשם ב-`recordEvent` רק כש-`kind === 'generation'`),
  כדי שהמונה יעקוב אחרי **תוצרים** ולא אחרי קריאות AI עזר/מדיה.
- ה-actor key מ-`actorKey()` — `user:<id>` אם יש, אחרת `phone:<...>` (WhatsApp).
- נקרא בנתיב ההפקה הראשי: `worker.ts:509` — `enforceAiLimit(database, actor, { kind: 'generation' })`.
  בהגעה למכסה → `AbuseGuardError` → `worker.ts` תופס → הבקשה עוברת ל-`needs_attention`.
- `SettingsPage` — עודכן טקסט העזר (הוסר "נשמר אך אינו נאכף כרגע").

`rate_limit_events` הוא free-form (`event_type` text) — אין צורך במיגרציה.
ה-cron ב-`conversation-maintenance` כבר גוזם רשומות ישנות מ-48ש'.

### עדיין פתוח

מגבלה **חודשית פר-משתמש** (180 גרפיקות, 6 מצגות, 25–30 מסמכים, 100 העלאות —
מהתמלול, לצורך הצעת מחיר) — לא קיימת. `generations_per_24h` הוא חלון מתגלגל
של 24ש', לא חודש קלנדרי.

---

## סטטוס פריסה

- `main`: `2ab1f83`.
- Edge functions: 37 פונקציות פרוסות ל-`tgropjisnheppsxejfdn`.
  `greenapi-status` + `whatsapp-group-webhook` הוסרו גם מה-remote.
- Frontend: `npx tsc --noEmit` ✅ · `npm run build` ✅
- `deno check` על הקבצים שנגעתי בהם ✅ (3 שגיאות טיפוס קדומות ב-`pdf.ts` /
  scheduling / `smartsend-webhook` brand-cast — קיימות גם ב-HEAD, לא בסשן הזה).

---

## פריטים שנשארו מהתמלול (לא בסשן הזה)

ראה טבלה מלאה בשיחת הסשן. עיקריים:

- קישור כניסה חד-פעמי + כפתור "אפס סיסמה" ייעודי במסך פרטי משתמש.
- מסך "פרטי פעילות": תפקיד מהאונבורדינג, טלפון, כמות יצירות, עלויות; כפתור
  "כניסה למשתמש" (impersonation).
- דשבורד אדמין: הוצאות חודש, כמות משתמשים, גרפים, מסננים, פילוח סוג תוצר.
- מגבלות חודשיות פר-משתמש + פר סוג תוצר.
- גאנט: תמונה אסינכרונית ברקע, Reel/Story, סגירת לופ האישור, "פרסם הכל",
  אי-המצאת אירועים כשמספר לא תואם.
- סקרייפינג אתר/רשתות למילוי מותג.
- באגים בחיבור Meta: תמונות ווטסאפ לא מצורפות, לוגו Autopost ב-authorization,
  error/מסך שחור, אינסטגרם, multi-select דפים.
