# סשן 31.08.2026 — Smart Send, תיקוני פלואו ובידוד בין מותגים

תיעוד מלא של הסשן: מה נבדק, מה נמצא, אילו החלטות התקבלו ולמה.
כל ממצא כאן מגובה בלוגים, בקוד או בבדיקה שרצה בפועל.

---

## 1. חסימת חלון 23 השעות

**הדרישה:** אסור לשלוח הודעה יוצאת למספר שלא כתב לנו ב־23 השעות האחרונות.
חריגה מזה חושפת את החשבון לחסימה מצד Meta.

**ההחלטה:** שער קשיח בנקודת השליחה עצמה, לא בפלואו.
פלואו אפשר לעקוף; נקודת שליחה אחת — לא.

`assertRecentInbound` ב־`_shared/whatsapp.ts`, נקרא גם מ־`sendText` וגם מ־`sendFile`.

**עקרון: fail-closed.** שלושה מצבים חוסמים, לא רק אחד:

| מצב | תוצאה |
|---|---|
| אין שיחה למספר | חסום |
| אין הודעה נכנסת בחלון | חסום |
| שגיאת DB בבדיקה | **חסום** |

הצלחת ה־API של הספק אף פעם לא נחשבת אישור.

**אימות בפרודקשן, בלי לשלוח דבר:**
- `+972549020253`, `+972502032767` — הודעות נכנסות באותו יום, עוברות.
- `+972500000000` — `conversations` מחזיר `[]`, חסום.

---

## 2. מדיה נכנסת — תמונה ואודיו

**התסמין:** תמונות ואודיו נדחו.

**האבחון.** הדחיות לא נרשמות ב־`messages` אלא ב־`logs`. חיפוש בטבלה הלא נכונה
הוביל למסקנה שגויה שהמדיה לא הגיעה כלל. היא כן הגיעה:

```
14:22:40  תמונה   media_failed    blocked_outbound_url: cdn.smartsend.co.il
14:26:20  תמונה   media_rejected  contentType: "image"
14:30:45  אודיו   media_rejected  contentType: "audio"
```

שתי סיבות נפרדות:
1. `cdn.smartsend.co.il` לא היה ברשימת הדומיינים המותרת ב־`safeFetch`.
2. Smart Send שולח **קטגוריה גנרית** (`image`, `audio`) בשדה `media_type`,
   לא MIME תקין. `isAllowedMime` דחה אותה.

**התיקון** (`smartsend-webhook/index.ts:138`): אם `media_type` לא מכיל `/`,
לוקחים את ה־`content-type` מתשובת ה־CDN.

**אימות אחרי הפריסה:**

| | מגיע | MIME | עיבוד | storage |
|---|---|---|---|---|
| תמונה | ✅ | `image/jpeg` | GPT מתאר, 333 תווים | ✅ נשמר |
| אודיו | ✅ | `audio/mp4` | תומלל | לא, **בכוונה** |

אודיו לא נשמר ב־storage — רק מתומלל. זו התנהגות מתוכננת ב־`inbound_media.ts:78`,
לא באג.

---

## 3. פלייסהולדרים לא מרונדרים — הבאג שהפיל את מכונת המצבים

**התסמין:** הבוט "מיהר באמצע הפלואו", לא חיכה לתשובה, וייצר תוצר מומצא.

**האבחון.** בכל הודעה עם מדיה, Smart Send שלח את הטוקן של Make כערך:

```
14:22:39  {{last_message}}   ← תמונה
14:30:43  {{last_message}}   ← אודיו
14:47:09  media_failed: blocked_outbound_url: unparseable   ← {{media_url}}
```

טקסט רגיל הגיע תקין. רק הודעות מדיה נפגעו.

**למה זה הרס את הפלואו.** ב־`inbound.ts` יש חמישה שערים דטרמיניסטיים —
ברכה, תפריט, reset, זיהוי מייל — וכולם מותנים ב־`numMedia === 0`.
`media_url` מזויף גרם ל־`numMedia = 1`, וכל השערים נעקפו.
לחיצת `1` בתפריט לא נקראה כבחירה; היא נפלה ישר לצינור ה־AI.

**אימות שהקוד שלנו לא השתנה.** `flow.ts` ו־`inbound.ts` — אפס שינויים מאז 20/08.
הריצה בסימולטור ב־23/08 חיכתה כמצופה:

```
1  →  "מעולה, תמונה או פוסט 🖼️ ספרו לי בקצרה מה להכין"   ← חיכה
```

**ההחלטה:** לתקן אצלנו, בנרמול, למרות שהבאג בצד Smart Send.
טוקן `{{...}}` נחשב מידע חסר, לא תוכן.

הבדיקה יושבת בתוך `firstString`, כך שהיא חלה על כל השדות בבת אחת —
`body`, `media_url`, `media_type`, `phone`. שדה פלייסהולדר נופל לשדה הבא;
payload שכולו פלייסהולדרים אינו הודעה.

---

## 4. הבריף הריק — "הכול הולך לקליניקה"

**התסמין:** לא משנה מה נשלח, חוזר אותו פוסט על קליניקה במגדל העמק.

**שני שורשים שהתחברו:**

1. **`priorContext` מזין את התוצר הקודם למנתח.** `worker.ts:521` שולף עד
   16 הודעות קודמות מהשיחה, כולל הפלט הקודם. בבקשה ריקה המנתח ממלא את
   `goal` מהתוצר הקודם. הקליניקה לא הומצאה מחדש — היא הועתקה.

2. **`forceBriefReady` רץ ללא תנאי.** גם כשהמנתח אמר "לא מוכן", הבריף סומן
   מוכן בכוח. מקורו בקומיט `8c0b776` מ־27/06, שהחליף את מסלול האישור הישן
   (`formatBriefForApproval` + `sendApprovalPrompt`) וגם ביטל את שכבות ה־QA.

**ההחלטה:** זרימה מהירה פירושה "לא לחקור את המשתמש", לא "להמציא תוצר".

התנאי לשאול דורש **שני סימנים שמסכימים**:
- המנתח אומר שהבריף לא מוכן, **וגם**
- המשתמש לא נתן מספיק בפועל

`ownBriefContent` מודד את מה שהמשתמש כתב **בבקשה הזאת בלבד**, ובמכוון מתעלם
מפלט המנתח — בדיוק בגלל שורש 1. ספרות תפריט, ברכות ואישורים נזרקים.
הסף: 25 תווים. בקשה אמיתית נוקבת בנושא; `"טיול נופים ברחבי מגדל העמק"` הוא 26.

בריף ריק מחזיר עכשיו:

> לא הבנתי מה להכין — הבקשה הגיעה בלי פרטים. אפשר לכתוב לי במשפט אחד
> מה התוצר, למי הוא מיועד ומה חשוב שיופיע בו?

**בקשה ענייניית לא מושפעת.** הזרימה המהירה נשארת בדיוק כשהייתה.

---

## 5. מדיה יוצאת — מיפוי ה־API של Smart Send

**השאלה:** איך שולחים תמונה כ־attachment בשיחה.

**המיפוי** (Swagger רשמי + בדיקת מסלולים ישירה). תחת
`/integrations/make/messages/` קיימים **בדיוק שלושה** מסלולים:

```
400  send-text              validation failed
400  send-template          validation failed
400  send-template-base64   Phone number and templateName are required
404  send-media / send-file / send-image / send-document / send-base64 / ...
```

`send-template-base64` הוא היחיד שנושא קובץ. הסכמה חולצה מהודעות השגיאה:
`{ phoneNumber, templateName, fileData, fileName }`.

`api.smartsend.co.il` חושף גם את `/whatsapp/api/Action/send-message-base64`
עם `mediaBase64` — מדיה חופשית **בלי תבנית**. הוא מחזיר `401` עם מזהה הארגון
שלנו בכל ארבע צורות ההזדהות שנוסו. דורש אישורים נפרדים.

**ההחלטות:**

- **base64, לא URL.** ה־signed URL לא עוזב את המערכת. Signed URL של Supabase
  פג תוקף ו־WhatsApp מושך מדיה מאוחר יותר — זה היה מקור שגיאות ה־`InvalidJWT`.
- **caption כהודעה נפרדת.** גוף התבנית נעול באישור Meta.
- **כשל רועש בלי `SMARTSEND_MEDIA_TEMPLATE`.** השולח נופל חזרה לטקסט.
  caption לעולם לא מתחזה לתמונה שנשלחה.
- תקרה 10MB, תואם ל־`MAX_MEDIA_BYTES`.

**חסם פתוח:** אין תבנית מאושרת. הקוד פרוס ומחכה. שתי דרכי פתיחה, לפי עדיפות:
1. אישורים ל־`Action/send-message-base64` — מדיה חופשית, בלי אישור Meta.
2. תבנית מאושרת עם IMAGE header → `supabase secrets set SMARTSEND_MEDIA_TEMPLATE=...`

---

## 6. בידוד בין מותגים ובין משתמשים

**הדרישה:** אסור שפרומפטים או תכנים יזלגו בין מותגים. אולי גם בין משתמשים.
התרחיש: אדם אחד, שני טלפונים — אחד למגדל העמק, אחד לתל אביב.

### מה נמצא בקוד

`worker.ts:161` — `matchBrand` שאל את **כל** המותגים הפעילים:

```ts
const { data: brands } = await database
  .from('brands').select('id, name, aliases').eq('is_active', true);
return matchBrandInText(brands, inboundText);
```

`user_brands` הופיע ב־`flow.ts` בלבד. כל צינור ה־worker — שיוך מותג, אישור,
`loadLearnedRules`, `buildBusinessBrainContext` — לא נגע בו אף פעם.

**התרחיש עבד בפועל:** משתמש ששייך רק לתל אביב כותב "מגדל העמק" → `brand_id`
נקבע למגדל העמק → נטענים לפרומפט עד 40 כללים נלמדים, עד 12 מקורות תוכן
מהמוח העסקי, וערכת המותג. ובכיוון ההפוך, `extractAndStoreRule` כותב כלל
**קבוע** על מותג שהמשתמש לא מורשה לו.

### מה עושים בעולם

הקונצנזוס: **גבול הבידוד נאכף בשכבת האחסון, לא בפילטר שצריך לזכור.**
אם ההגנה היחידה היא `WHERE brand_id = ?` שמישהו צריך לזכור לכתוב — תדלוף.

בין משתמשים באותו ארגון, התעשייה דווקא **מאחדת בכוונה** — Zoho Desk, Fin,
Intercom: מספר משותף, inbox מאוחד, רציפות שירות בין נציגים.

### ההחלטה — אושרה במפורש על ידי בעל המוצר, 31.08.2026

**גבול הבידוד הוא המותג. לא המשתמש.**

| | מופרד? | נימוק |
|---|---|---|
| **בין מותגים** | **כן, מוחלט** | אין תרחיש לגיטימי. פרטיות, איכות תוצר וחשיפה משפטית |
| היסטוריית שיחה בין משתמשים | **כן** — כבר קיים | שיחה נקשרת למספר טלפון; `priorContext` מסונן ב־`conversation_id` |
| מוח עסקי, צבעים, כללים נלמדים | **לא — במכוון** | נכס של המותג. הפרדה לפי משתמש תגרום לכל עובד חדש לחזור על אותן טעויות — בדיוק מה שסקיל 10 בא למנוע |

**מה זה אומר בפועל.** שני עובדים של מגדל העמק:

- **חולקים:** כללים שנלמדו מתיקונים, מוח עסקי, ערכת מותג, צבעים, לוגו.
  תיקון שאחד לימד את המערכת חל גם על השני.
- **לא חולקים:** היסטוריית שיחה. מה שאחד כתב בשיחה שלו לא נחשף לשני,
  כי שיחה נקשרת למספר טלפון.

זה תואם את מה שהתעשייה עושה (Zoho Desk, Fin, Intercom): שיתוף נכסי ארגון,
הפרדת שיחות.

**מתי לחזור לזה:** אם ייכנס לתמונה מותג שבו עובדים שונים לא אמורים לראות
זה את התיקונים של זה — למשל סוכנות שמנהלת כמה לקוחות תחת מותג אחד.
אז `brand_learned_rules` יצטרך גם `user_id`. כרגע לא זה המקרה.

### ארבע השכבות שהוטמעו

**שכבה 1 — הגבלת ההתאמה.** מודול חדש `_shared/brandAccess.ts`.
המועמדים מסוננים **לפני** התאמת השם, לא אחריה: מותג זר לא יכול לנצח התאמה
מלכתחילה, ולכן לא יכול להיות מוצע, מאושר או נכתב אליו.
בקשה בלי בעלים מתאימה לכלום, לא לכולם.

**שכבה 2 — שער בכתיבה.** `extractAndStoreRule` מקבל `userId` ומאמת חברות
לפני כתיבת כלל. כלל נלמד הוא קבוע וחוצה־מותג, ולכן הכותב חייב להיות חבר.
סירוב נרשם כ־`brand_rule_write_denied`.

גם אישור `pending_brand_id` נבדק מחדש: "כן" של משתמש לא יכול להיות הדבר
שמעניק גישה.

**שכבה 3 — RLS.** נבדק מול הפרודקשן:

| טבלה | RLS | מדיניות |
|---|---|---|
| `brands` | ✅ | `user_brands` |
| `business_text_sources` | ✅ | `user_brands` (select/update/delete) |
| `outputs` | ✅ | `is_brand_member(brand_id)` + יוצר |
| `requests` | ✅ | `is_brand_member(brand_id)` + יוצר |
| `user_brands` | ✅ | `user_id = auth.uid()` |
| `brand_learned_rules` | ✅ | admin בלבד |
| `conversations`, `messages` | ✅ | admin בלבד |

אין טבלה עם RLS כבוי. שלוש טבלאות עם RLS דלוק ואפס מדיניות
(`annual_planner_content_cache`, `rate_limit_events`, `session_devices`) —
כלומר deny-all, מגביל ולא דולף. anon מחזיר `rows=0` בכל הטבלאות.

**שכבת ה־RLS תקינה.** הפער היחיד היה שה־Edge Functions רצות עם `service_role`
שעוקף RLS — וזה בדיוק מה ששכבות 1–2 סוגרות.

**שכבה 4 — בדיקות `store-as-A, read-as-B`.** `brandAccess_test.ts`, שני
משתמשים על שני מותגים:

- דנה → מגדל העמק. יוסי → תל אביב.
- יוסי לא מגיע למגדל העמק, ודנה לא לתל אביב.
- הזכרת המותג האחר לא הופכת אותו למועמד.
- משתמש בלי שיוך מתאים לכלום, לא לכולם.
- **כשל בבדיקת החברות אוסר, לא מתיר.**
- בקשה בלי בעלים אינה wildcard (`null`, `undefined`, `''`).
- משתמש המשויך לשני המותגים שומר גישה לשניהם.

---

## מצב סופי

**בדיקות:** 28/28.

`deno check` מחזיר 3 שגיאות טיפוס ב־`pdf.ts`. אומתו מול `git stash` —
**קיימות גם ב־HEAD, לפני שינויי הסשן.** לא נגעתי. הבדיקות רצות ב־`--no-check`.

### תוצר שנמסר מוסר מהקשר המנתח

הפתוח שנשאר מסעיף 4 — `priorContext` שהזין את התוצר הקודם למנתח — נסגר.

**האבחון:** תוצר גמור הוא טקסט ארוך, בטוח ותואם־מותג. כשמחזירים אותו למנתח
לצד בקשה חדשה ודלה, הוא הדבר הכי אטרקטיבי בחלון להעתקה. המנתח משלים ממנו
את הבריף. זה מה שגרם לפוסט הקליניקה לחזור כתשובה להודעות לא קשורות.

**ההחלטה:** `conversationContextMessages` מסיר הודעות **יוצאות** ששייכות
לבקשה שכבר הפיקה תוצר (`outputs`). נשמר:

- כל מה שהמשתמש כתב — זה מה שנושא את ההמשכיות ("תעשה עוד אחד כזה")
- שאלות, תפריטים ואישורים — קצרים, נושאים את מצב הדיאלוג, ואין מה להעתיק מהם
- הודעות ברמת השיחה בלי `request_id`
- בקשה שלא הפיקה כלום — הדיאלוג שלה נשאר

זה גבול **איכות ורלוונטיות, לא גבול דיירוּת**. הפילטר תמיד היה מוגבל ל־
`conversation_id` אחד, ושיחה שייכת למספר טלפון אחד — הוא מעולם לא חצה
משתמשים או מותגים.

**קומיטים על `main`:**

| | |
|---|---|
| `5f36010` | חסימת חלון 23 שעות |
| `7d3dd2d` | סינון פלייסהולדרים לא מרונדרים |
| `a8d50d2` | שליחת מדיה ב־base64 |
| `08dda61` | deno.lock |
| `5ebe2dd` | בריף ריק — לומר, לא להמציא |

**פריסה:** כל 38 הפונקציות נפרסו מהריפו ואומתו ACTIVE. אין פער בין הקוד לפרודקשן.

---

## פתוח

1. **תבנית מדיה מ־Smart Send** — חוסם שליחת תמונה. ראה סעיף 5.
2. **באג הפלייסהולדרים בצד Smart Send** — מסונן אצלנו, אבל שווה שיתקנו במקור.
3. **שכבות ה־QA שבוטלו ב־`8c0b776`** — `runQa` ו־`reviewImageQa` הוסרו יחד עם
   מסלול האישור. אף אחד לא ביקש להחזיר אותן; מתועד כדי שההחלטה תהיה מודעת.
